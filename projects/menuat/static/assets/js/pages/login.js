$(document).ready(function(){
	/* ---------- Placeholder Fix for IE ---------- */
	$('input').iCheck({
		checkboxClass: 'icheckbox_square-blue'
	});
	$.backstretch([
		"/assets/img/lockscreen/menuat-pattern.gif"
	], {
	    fade: 1000,
	    duration: 4000
	});
});